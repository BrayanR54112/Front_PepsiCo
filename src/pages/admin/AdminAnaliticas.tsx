import { Analiticas } from "../Analiticas";

export function AdminAnaliticas() {
  // Reusing the same analytics component for demo purposes
  // In a real app we'd fetch global data here instead of store-specific
  return <Analiticas />;
}
